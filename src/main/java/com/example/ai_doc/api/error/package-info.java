/**
 * The application's exception types and the single advice that turns them into
 * HTTP responses.
 *
 * <p>Every exception thrown deliberately by the pipeline lives here, so the set of
 * failures a client can observe is readable in one place.
 */
package com.example.ai_doc.api.error;
