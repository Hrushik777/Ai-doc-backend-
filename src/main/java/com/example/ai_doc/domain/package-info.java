/**
 * Pure value types - records with no Spring, JPA, or HTTP awareness.
 *
 * <p>These are the vocabulary the pipeline speaks. They are safe to construct in a
 * test without a context, which is what makes the layout and mapping stages
 * testable at all.
 */
package com.example.ai_doc.domain;
